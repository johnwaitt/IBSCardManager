@model IBSCardManager.Entities.Card

@{
    ViewData["Title"] = "Add Card";
    ViewData["FormAction"] = "Create";
    ViewData["SubmitText"] = "Create Card";
}

<partial name="_CardForm"
         model="Model"
         view-data="ViewData" />

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}